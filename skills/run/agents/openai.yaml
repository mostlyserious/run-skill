interface:
  display_name: "Run"
  short_description: "Plan and run multi-step project sessions"
  default_prompt: "Use $run to plan a substantial project, then hand back a launch-ready run package."
policy:
  allow_implicit_invocation: true
